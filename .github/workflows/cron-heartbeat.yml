name: Cron Heartbeat

on:
  workflow_dispatch:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  heartbeat:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduler bestätigen
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Zeitpunkt UTC:"
          date -u
